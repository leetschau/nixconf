# Gemini CLI 项目指令 (GEMINI.md)

此文件包含针对此工作区的特定指令。Gemini CLI 将优先遵循此处的规则。

## 角色与上下文
<!-- 
在此处定义 AI 的角色定位或项目背景。
例如：你是一个专门负责 NixOS 配置优化的专家。
-->

## 项目规范
<!-- 
设定代码风格、提交规范或特定的技术要求。
-->
- **配置风格**: 优先使用模块化的 Nix 配置，保持 `configuration.nix` 简洁。
- **安全性**: 严禁在任何 `.nix` 文件中明文写入密钥，优先使用 sops-nix 或 agenix。

## 工作流指令
<!-- 
定义特定的操作流程。
-->
- **验证**: 在修改 Nix 配置后，始终建议运行 `nixos-rebuild dry-activate` 进行测试。
- **文档**: 为复杂的 Nix 函数或自定义模块添加必要的注释。

## 禁用规则 (可选)
<!-- 
明确禁止 AI 执行的操作。
- 严禁未经确认直接执行 nixos-rebuild switch。
-->
